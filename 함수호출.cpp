/*
함수호출
- 값 전달 방식
  - 값에의한 호출, call by value
  - 주소에 의한 호출, call by address
  - 참조에 의한 호출, call by reference

-값에 의한 호출
  -호출하는 코드에서 넘어온 값을 매개 변수에 복사함(스택 이용)
 
-주소에 의한 호출
  -값을 가지고 있는 변수의 주소를 포인터 변수를 통해 전달

- 참조에 의한 호출
  -참조 매개 변수의 이름만 사용하고 별도 공간이 생기지 않음
   
*/